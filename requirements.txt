# Ranges rather than hard pins: the binary wheels available differ per
# Python version and platform, and a hard pin silently excludes the
# machine you are sitting at. Upper bounds keep major versions honest.
requests>=2.32,<3
python-dotenv>=1.0,<2
psycopg[binary]>=3.2.4,<4
pandas>=2.2,<3
matplotlib>=3.9,<4
dbt-postgres>=1.8,<2

# Only needed for --target databricks; the daily run uses Postgres.
dbt-databricks>=1.8,<2
