# data
data/raw/**
!data/raw/.gitkeep
data/processed/**

# snapshots are the time series, and small enough to keep
!data/snapshots/**

# charts are output, but they are what the README shows
!analysis/charts/*.png

# python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# dbt
dbt/target/
# docs/ is generated but committed - GitHub Pages serves it
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# os / editor
.DS_Store
.idea/
.vscode/

# private notes, kept locally on purpose
ENGINEERING-LOG.md

# desktop app drops files here; not project content
Claude outputs/

# handoff notes for a fresh session, kept locally
HANDOFF.md
